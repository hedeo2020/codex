import {describe,it,expect,beforeAll} from "vitest";import {cosineSimilarity,encryptBiometricTemplate,decryptBiometricTemplate,redactSensitive} from "../src/lib/security";
beforeAll(()=>{process.env.BIOMETRIC_ENCRYPTION_KEY=Buffer.alloc(32,7).toString("base64")});
describe("biometric security",()=>{it("encrypts and decrypts a template",()=>{const source=new Float32Array([.1,.2,.3]);const encrypted=encryptBiometricTemplate(source);expect(encrypted.ciphertext.equals(Buffer.from(source.buffer))).toBe(false);expect(Array.from(decryptBiometricTemplate(encrypted))).toEqual(Array.from(source))});it("compares embeddings",()=>expect(cosineSimilarity(new Float32Array([1,0]),new Float32Array([1,0]))).toBeCloseTo(1));it("redacts sensitive fields recursively",()=>expect(redactSensitive({email:"a@b.c",encryptedEmbedding:"secret",nested:{token:"secret"}})).toEqual({email:"a@b.c",nested:{}}))});
